CRITICAL GAME LOGIC FIX.

DO NOT rebuild the project.

The current implementation is WRONG in three important areas:

1. Free Spins trigger immediately starts the first Free Spin instead of showing a proper awarded-bonus / START screen.
2. The raw wins of one Free Spin tumble sequence are not accumulating visibly into one tumble-win pool.
3. Multiplier Cores are multiplying only a local/current tumble instead of multiplying the COMPLETE accumulated tumble-sequence win.

STOP adding visual patches around the existing implementation.

FIX THE UNDERLYING STATE MACHINE AND PAYOUT ENGINE.

The behavior described below replaces all conflicting previous multiplier / Free Spin behavior.

==================================================
1. DEFINE THE CORRECT ROUND TERMINOLOGY
==================================================

A FREE SPIN consists of:

INITIAL BOARD
+
ZERO OR MORE WINNING TUMBLES
+
ALL REFILLS
+
THE FINAL NON-WINNING BOARD

That entire chain is ONE:

TUMBLE SEQUENCE

Example:

Free Spin starts
↓
Initial board wins 1.20x
↓
Tumble
↓
New board wins 2.80x
↓
Tumble
↓
New board wins 4.00x
↓
Tumble
↓
No more win
↓
SEQUENCE ENDS

Raw Tumble Sequence Win:

1.20 + 2.80 + 4.00
=
8.00x

That 8.00x is the WIN POOL for that Free Spin sequence.

==================================================
2. NEW REQUIRED STATE MACHINE
==================================================

Implement these states explicitly:

BASE_IDLE

BASE_SPIN

BASE_TUMBLE

BONUS_TRIGGERED

BONUS_AWARD_PRESENTATION

BONUS_WAITING_FOR_START

BONUS_TRANSITION

FREE_SPIN_READY

FREE_SPIN_INITIAL_DROP

FREE_SPIN_EVALUATE

FREE_SPIN_WIN

FREE_SPIN_TUMBLE

FREE_SPIN_REFILL

FREE_SPIN_SEQUENCE_SETTLEMENT

FREE_SPIN_COMPLETE

BONUS_COMPLETE

BONUS_RETURN_TO_BASE

The following transition must NEVER happen:

BONUS_TRIGGERED
->
FREE_SPIN_INITIAL_DROP

That direct transition is forbidden.

Correct:

BONUS_TRIGGERED
->
BONUS_AWARD_PRESENTATION
->
BONUS_WAITING_FOR_START
->
USER PRESSES START
->
BONUS_TRANSITION
->
FREE_SPIN_READY
->
FREE_SPIN_INITIAL_DROP

==================================================
3. FREE SPINS MUST NOT START AUTOMATICALLY
==================================================

THIS IS MANDATORY.

When 4+ qualifying Scatter symbols trigger Free Spins:

DO NOT start the first Free Spin automatically.

DO NOT use a 2 second automatic timeout.

DO NOT silently continue.

DO NOT deduct a Free Spin.

DO NOT generate the first Free Spin board yet.

Instead freeze the game after Scatter celebration.

Show a dedicated full-screen award screen.

==================================================
4. FREE SPIN AWARD SCREEN
==================================================

After Scatter celebration display:

FREE SPINS WON

then prominently:

10 FREE SPINS

or whatever amount was awarded.

Below it show a large button:

START FREE SPINS

The player MUST press this button.

There must be NO automatic continuation.

Screen example:

--------------------------------

FREE SPINS WON

10
FREE SPINS

[ START FREE SPINS ]

--------------------------------

This must be a real game state, not an overlay placed while gameplay continues behind it.

While this screen is open:

RNG must not generate Free Spin results.

No Free Spin counter decreases.

No tumble starts.

No board changes.

No Autoplay action occurs.

No bet is deducted.

Only after START FREE SPINS is pressed can the bonus proceed.

==================================================
5. START BUTTON FEEDBACK
==================================================

When player presses:

START FREE SPINS

then:

button reacts
bonus music rises
gold transition begins
award screen fades
bonus board atmosphere appears

THEN initialize the first Free Spin.

Prevent double-click.

START must only fire once.

==================================================
6. BONUS START SCREEN MOBILE DESIGN
==================================================

Mobile first.

At 390x844 the screen must clearly show:

FREE SPINS WON

large:
10

FREE SPINS

large premium:
START FREE SPINS

The Start button must be comfortably tappable.

Minimum approximate touch target:

56px height.

Do not make this a tiny modal.

It should feel like a major game event.

==================================================
7. INTRO ANIMATION
==================================================

Before showing START:

Scatter celebration can play.

Sequence:

4th qualifying Scatter lands

gold impact

Scatter symbols pulse

short winning fanfare

screen darkens slightly

FREE SPINS WON panel enters

Then gameplay STOPS and waits indefinitely for user input.

No timer auto-start.

==================================================
8. CRITICAL PAYOUT MODEL
==================================================

Create these separate variables for every Free Spin:

freeSpinSequenceRawWin
freeSpinSequenceMultiplierTotal
freeSpinSequenceFinalWin

Also maintain:

bonusTotalWin

They are NOT interchangeable.

==================================================
9. RAW WIN POOL
==================================================

At the beginning of EACH NEW Free Spin:

freeSpinSequenceRawWin = 0

freeSpinSequenceMultiplierTotal = 0

activeMultiplierCores = []

Then initial board is generated.

Every winning tumble contributes its RAW NORMAL-SYMBOL payout to:

freeSpinSequenceRawWin

Example:

Tumble 1:

1.20x

pool becomes:

1.20x

Tumble 2:

2.80x

pool becomes:

4.00x

Tumble 3:

4.00x

pool becomes:

8.00x

The UI must show this accumulating value.

==================================================
10. TUMBLE WIN POOL UI
==================================================

During the entire Free Spin sequence prominently show directly above or below the board:

TUMBLE WIN

1.20x

Then after next win:

TUMBLE WIN

4.00x

Then:

TUMBLE WIN

8.00x

IMPORTANT:

THIS NUMBER IS CUMULATIVE FOR THE CURRENT FREE SPIN SEQUENCE.

Do NOT replace it with only the latest individual win.

If tumble #2 pays 2.80x:

do NOT display:

2.80x

as the primary TUMBLE WIN.

Animate:

1.20x
+
2.80x
->
4.00x

==================================================
11. TEMPORARY INDIVIDUAL WIN
==================================================

You may additionally show a small temporary:

+2.80x

near the winning symbols.

But the main pool remains:

TUMBLE WIN
4.00x

This makes it obvious that winnings are accumulating.

==================================================
12. EXAMPLE REQUIRED VISUAL FLOW
==================================================

First win:

+1.20x

TUMBLE WIN
1.20x

Second win:

+2.80x

TUMBLE WIN counter animates:

1.20
->
4.00

Third win:

+4.00x

TUMBLE WIN:

8.00x

This value stays visible while the tumble sequence continues.

==================================================
13. MULTIPLIER CORE DOES NOT IMMEDIATELY PAY
==================================================

Multiplier Core landing MUST NOT immediately modify money.

Example:

TUMBLE WIN currently:

4.00x

Then a:

25x

Core falls.

DO NOT change:

4.00x -> 100x

yet.

DO NOT credit money.

DO NOT multiply only the next winning tumble.

The Core becomes stored in:

activeMultiplierCores

and remains visually present.

==================================================
14. MULTIPLIER CORES PERSIST THROUGH SEQUENCE
==================================================

If:

10x Core

lands after tumble #1:

keep it.

If another win happens:

keep it.

If then:

25x Core

lands:

now activeMultiplierCores contains:

10x
25x

Both remain.

They stay until the CURRENT FREE SPIN TUMBLE SEQUENCE completely ends.

==================================================
15. IMPORTANT: MULTIPLIER APPLIES TO THE POOL
==================================================

When the tumble sequence finally ends:

take:

freeSpinSequenceRawWin

Then add all active Multiplier Core values.

Example:

raw win pool:

8.00x

Cores:

10x
25x

Multiplier total:

35x

Final sequence payout:

8.00x * 35

=

280.00x

THIS IS THE REQUIRED BEHAVIOR.

==================================================
16. THIS OLD BEHAVIOR MUST BE DELETED
==================================================

Find and remove any logic resembling:

currentTumbleWin * multiplier

or:

latestWin * multiplier

or:

winWhenCoreArrived * multiplier

or:

tumblePayout *= currentMultiplier

inside the individual tumble loop.

Multiplier settlement must NOT occur there.

Individual tumble evaluation only adds RAW payout to:

freeSpinSequenceRawWin.

==================================================
17. ONE AND ONLY ONE MULTIPLIER SETTLEMENT
==================================================

Multiplier math occurs exactly ONCE.

Location:

FREE_SPIN_SEQUENCE_SETTLEMENT

after:

the board has been evaluated
AND
there is no next normal-symbol win.

Pseudo behavior:

if noMoreWins:

    sequenceRawWin = accumulatedRawWin

    multiplierTotal =
        sum(activeMultiplierCores)

    if multiplierTotal > 0:
        finalSequenceWin =
            sequenceRawWin * multiplierTotal
    else:
        finalSequenceWin =
            sequenceRawWin

Then credit:

bonusTotalWin += finalSequenceWin

Then clear:

activeMultiplierCores
freeSpinSequenceRawWin
freeSpinSequenceMultiplierTotal

Then move to next Free Spin.

==================================================
18. DO NOT CREDIT RAW WINS EARLY
==================================================

During a Free Spin tumble sequence:

individual wins should NOT be permanently credited to:

bonusTotalWin

one-by-one.

They are provisional RAW wins in:

freeSpinSequenceRawWin.

Only when the sequence settles is the final result credited.

Otherwise multiplier accounting can double-pay.

==================================================
19. NO DOUBLE PAYMENT
==================================================

This is critical.

Wrong:

1. Add 8x raw win to Bonus Win
2. Calculate 8x * 35 = 280x
3. Add 280x again

That would pay:

288x

WRONG.

Correct:

raw pool:
8x

not yet finalized

settlement:

8 * 35
=
280x

Bonus Win receives:

280x

exactly once.

==================================================
20. NO MULTIPLIER CASE
==================================================

Example:

raw pool:

8.00x

no Multiplier Cores.

Then final:

8.00x

Bonus Win increases by:

8.00x.

==================================================
21. ONE MULTIPLIER EXAMPLE
==================================================

Raw sequence:

2.40x
+
1.80x
+
3.00x

=

7.20x

Core:

10x

final:

72.00x.

NOT:

first wins normally
+
only last 3.00 * 10.

==================================================
22. MULTIPLE MULTIPLIER EXAMPLE
==================================================

Sequence:

Win A = 1.50x
Win B = 2.50x
Win C = 3.00x

Raw pool:

7.00x

Cores:

2x
10x
25x

Sum:

37x

Final:

7.00 x 37
=
259.00x

Credit exactly:

259x.

==================================================
23. CORE LANDING LATE IN THE SEQUENCE
==================================================

This is important.

Example:

Tumble 1 wins:

2x.

Tumble 2 wins:

3x.

Raw Pool:

5x.

During the next refill:

a 25x Core lands.

Then the resulting board has NO further symbol win.

The sequence now ends.

Because the 25x Core is present at the end of the tumble sequence:

apply it to the COMPLETE accumulated pool:

5x * 25
=
125x.

DO NOT require a new symbol explosion after the Core arrived.

The Core being present at sequence settlement is sufficient.

==================================================
24. MULTIPLIER SETTLEMENT VISUAL
==================================================

Sequence end should visibly pause before clearing the board.

Example display:

TUMBLE WIN

8.00x

Then Cores activate one at a time.

10x

energy animation.

25x

energy animation.

Then:

TOTAL MULTIPLIER

35x

Then:

8.00 x 35

Animate calculation.

Then large:

280.00x

Then TUMBLE WIN counter transforms into final value.

Then:

BONUS WIN

increases by:

280.00x.

==================================================
25. DO NOT REMOVE CORES BEFORE SETTLEMENT
==================================================

Current bug may be caused by clearing Cores too early.

Correct order:

NO MORE NORMAL WIN FOUND

↓

FREE_SPIN_SEQUENCE_SETTLEMENT

↓

READ activeMultiplierCores

↓

CALCULATE SUM

↓

ANIMATE CORES

↓

CALCULATE FINAL WIN

↓

CREDIT BONUS TOTAL

↓

REMOVE CORE VISUALS

↓

CLEAR CORE STATE

↓

NEXT FREE SPIN

Never clear Core state before math settlement.

==================================================
26. CORE VISUAL STATE MUST MATCH ENGINE STATE
==================================================

There must be ONE authoritative Core list.

Do not have:

visualCores[]

and unrelated:

mathMultipliers[]

that can disagree.

Use engine-owned data.

Renderer mirrors it.

Every visible Core must correspond to an active mathematical Core.

==================================================
27. TUMBLE WIN POOL POSITION
==================================================

The accumulated current sequence TUMBLE WIN must be extremely obvious.

Mobile target:

place immediately above the grid or immediately below it.

Suggested:

TUMBLE WIN
8.00x

The numeric value should be approximately:

28-40px visual size

on common mobile screens.

Do not place it in tiny HUD text.

==================================================
28. MULTIPLIER ARRIVAL SHOULD CREATE ANTICIPATION
==================================================

When a large Core arrives:

do not pay it immediately.

Player should SEE:

TUMBLE WIN
8.00x

and:

25x

sitting on board.

If another:

50x

arrives:

both stay visible.

This creates:

25x + 50x

while the TUMBLE WIN pool is still visible.

At sequence end:

75x applied to the pool.

==================================================
29. FREE SPIN COUNTER TIMING
==================================================

When entering bonus with:

10 FREE SPINS

the award screen displays:

10 FREE SPINS.

Do NOT show:

9

before the first spin has visibly started.

When player presses START:

show:

FREE SPINS
10

Then begin spin #1.

After spin #1 completely settles:

remaining becomes:

9.

The count should correspond to completed spins.

==================================================
30. RETRIGGER
==================================================

During Free Spins:

3+ Scatter on the INITIAL Free Spin board

awards:

+5 FREE SPINS.

The +5 must be added to the remaining counter.

Show:

+5 FREE SPINS

before continuing.

Do not automatically start another Free Spin while retrigger animation is active.

==================================================
31. FREE SPIN END
==================================================

After final Free Spin sequence settles:

DO NOT instantly return to base game.

Enter:

BONUS_COMPLETE.

Show:

BONUS COMPLETE

TOTAL WIN

xxx.xx

and:

xxx.x BET

Then require:

CONTINUE

preferred behavior:

explicit Continue button.

Do not auto-close unless an optional setting later requests it.

==================================================
32. STATE LOCKING
==================================================

While these screens are active:

BONUS_AWARD_PRESENTATION
BONUS_WAITING_FOR_START
FREE_SPIN_SEQUENCE_SETTLEMENT
BONUS_COMPLETE

disable:

Spin
Auto
Bet change
duplicate Start
duplicate Continue.

Prevent race conditions.

==================================================
33. PLAYER INPUT LOCK
==================================================

START FREE SPINS:

can fire exactly once.

CONTINUE:

can fire exactly once.

Use state transition guards.

Do not rely only on disabled CSS.

==================================================
34. ENGINE DATA MODEL
==================================================

Add a structure similar to:

FreeSpinSequenceState {
    rawWinMultiplier: number;
    activeMultiplierCores: MultiplierCore[];
    combinedMultiplier: number;
    finalWinMultiplier: number;
    tumbleIndex: number;
    settled: boolean;
}

Do not store core math only inside Phaser objects.

==================================================
35. INDIVIDUAL TUMBLE EVENT
==================================================

Each winning tumble should generate an event conceptually like:

{
    type: "TUMBLE_WIN",
    tumbleIndex: 2,
    rawWin: 2.8,
    previousSequenceRawWin: 1.2,
    newSequenceRawWin: 4.0
}

Renderer uses this to animate:

1.20
+
2.80
->
4.00.

==================================================
36. CORE ARRIVAL EVENT
==================================================

Example:

{
    type: "MULTIPLIER_CORE_LANDED",
    value: 25,
    activeCoreValues: [10, 25]
}

Do not calculate payout here.

==================================================
37. SEQUENCE SETTLEMENT EVENT
==================================================

When no more wins:

{
    type: "FREE_SPIN_SEQUENCE_SETTLED",
    rawWin: 8.0,
    multiplierValues: [10, 25],
    combinedMultiplier: 35,
    finalWin: 280.0
}

This should be the ONLY event that permanently adds the sequence result to Bonus Win.

==================================================
38. REQUIRED UNIT TEST: ACCUMULATING POOL
==================================================

Test:

tumble payouts:

1.2
2.8
4.0

Expected:

sequence raw pool:
8.0

Not:

4.0.

==================================================
39. REQUIRED UNIT TEST: LATE CORE
==================================================

Sequence:

first win:
2x

second win:
3x

then:

25x Core lands

no next win.

Expected:

raw pool:
5x

combined multiplier:
25x

final:
125x.

==================================================
40. REQUIRED UNIT TEST: MULTIPLE CORES
==================================================

Raw wins:

1.5
2.5
3.0

Expected raw:

7x.

Cores:

2
10
25

Expected combined multiplier:

37x.

Expected final:

259x.

==================================================
41. REQUIRED UNIT TEST: NO DOUBLE CREDIT
==================================================

Raw:

8x

Core total:

35x.

Expected Bonus Win increment:

280x.

NOT:

288x.

==================================================
42. REQUIRED UNIT TEST: NO CORE
==================================================

Raw:

8x

no Cores.

Expected:

8x.

==================================================
43. REQUIRED UNIT TEST: BONUS START BLOCK
==================================================

Trigger:

4 qualifying Scatters.

Expected state:

BONUS_WAITING_FOR_START.

Verify:

freeSpinBoardsGenerated = 0.

Wait arbitrary simulated time.

Expected:

still:

BONUS_WAITING_FOR_START.

Only dispatch:

START_FREE_SPINS

Then:

first Free Spin may be generated.

==================================================
44. REQUIRED UNIT TEST: DOUBLE START
==================================================

Press START twice rapidly.

Exactly one bonus transition.

Exactly one first Free Spin.

No counter corruption.

==================================================
45. REQUIRED UNIT TEST: BONUS COUNTER
==================================================

Award:

10 spins.

Before START:
10.

Immediately after START before first completion:
10.

After Free Spin #1 settlement:
9.

After Free Spin #2:
8.

==================================================
46. REQUIRED INTEGRATION TEST
==================================================

Create deterministic scenario:

Base board:
4 Scatter.

Expected:

Scatter celebration.

Then:

FREE SPINS WON
10 FREE SPINS
START FREE SPINS.

Nothing moves until Start.

Press Start.

Free Spin #1:

initial win:
1.20x.

TUMBLE WIN:
1.20x.

Next:
2.80x win.

TUMBLE WIN:
4.00x.

Refill:
10x Core.

Next:
4.00x win.

TUMBLE WIN:
8.00x.

Refill:
25x Core.

Final board:
no normal-symbol win.

Expected settlement:

TUMBLE WIN:
8.00x.

Cores:

10x + 25x.

TOTAL MULTIPLIER:
35x.

Calculation:

8.00 x 35.

FINAL:

280.00x.

BONUS WIN increments exactly:

280.00x.

Then next Free Spin.

This integration test MUST pass.

==================================================
47. DEBUG / LAB SCENARIO
==================================================

Add one-click /lab scenario named:

BONANZA-STYLE SEQUENCE SETTLEMENT TEST

It must reproduce:

1.20x
+
2.80x
+
4.00x

raw pool:

8.00x

and:

10x
+
25x

then:

280x final.

Allow us to visually verify the entire sequence.

==================================================
48. REMOVE CONFLICTING OLD LOGIC
==================================================

Search repository for every piece of previous multiplier implementation.

Remove or refactor conflicting code.

There must not be BOTH:

per-tumble multiplier logic

and:

sequence settlement multiplier logic.

Only the new sequence-settlement model may remain.

==================================================
49. DO NOT CHANGE RTP YET TO HIDE BUGS
==================================================

First make accounting correct.

First make tests pass.

Only then rerun simulator.

Do not adjust probabilities to compensate for incorrect calculations.

==================================================
50. UPDATE SIMULATOR
==================================================

Simulator must now model the exact same process:

For each Free Spin:

rawSequenceWin = 0

activeCores = []

generate initial board

while sequence continues:

calculate raw normal-symbol win

rawSequenceWin += rawWin

generate tumbles/refills

collect multiplier cores

when no more normal win:

combinedMultiplier =
    sum(activeCores)

if combinedMultiplier > 0:

    finalSequenceWin =
        rawSequenceWin * combinedMultiplier

else:

    finalSequenceWin =
        rawSequenceWin

credit finalSequenceWin once.

==================================================
51. SIMULATION METRICS
==================================================

Add:

Average raw Free Spin sequence win

Average final sequence win

Average multiplier uplift

Percentage sequences with multiplier

Percentage sequences where multiplier lands AFTER at least one raw win

Average active Cores at settlement

Average combined multiplier

Max combined multiplier

Raw Bonus RTP

Multiplier-added Bonus RTP

This helps confirm the engine is behaving correctly.

==================================================
52. VISUAL REQUIREMENT
==================================================

At any moment during a Free Spin tumble sequence player should understand:

HOW MUCH RAW MONEY IS CURRENTLY IN THE POOL

and:

WHICH MULTIPLIERS ARE CURRENTLY WAITING.

Example on phone:

TUMBLE WIN
8.00

board contains:

10x
25x

Player can visually understand:

"I currently have 8.00 in the tumble pool and 35x waiting."

Then settlement occurs.

==================================================
53. DO NOT FINISH UNTIL VERIFIED
==================================================

Before completion:

run deterministic integration test.

Manually launch /lab scenario.

Verify bonus start screen waits for input.

Verify raw tumble pool visibly accumulates.

Verify late multiplier applies to earlier accumulated tumble wins.

Verify multiple multipliers add.

Verify exact payout.

Then run:

npm test

Then:

npm run build

Then rerun mathematical simulation.

==================================================
54. FINAL REPORT
==================================================

Tell me explicitly:

BONUS START:
Does the game now wait indefinitely at START FREE SPINS?

TUMBLE POOL:
Does the displayed value accumulate every raw tumble win?

MULTIPLIER:
Is the combined multiplier applied ONCE to the complete accumulated tumble sequence?

LATE MULTIPLIER:
Can a multiplier arriving late multiply raw wins earned earlier in the same tumble sequence?

DOUBLE PAY:
Is raw win prevented from being credited separately before final multiplier settlement?

TEST:
Did the deterministic 8.00x + 35x = 280x scenario pass?

Do not answer "implemented" without verifying each item.