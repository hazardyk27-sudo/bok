{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "vite --host 0.0.0.0 --port 5000",
    "build": "pnpm run typecheck && vite build",
    "test": "vitest run",
    "simulate": "tsx src/simulation/cli.ts",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && tsc -p tsconfig.app.json --noEmit && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.3",
    "@types/node": "catalog:",
    "@types/ws": "^8.18.1",
    "happy-dom": "^20.14.5",
    "phaser": "^3.90.0",
    "tsx": "catalog:",
    "vite": "catalog:",
    "vitest": "^5.0.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
